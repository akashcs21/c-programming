#include<stdio.h>
#include<math.h>
#include<string.>
int main{
  unsigned long long  int n,i;
    printf("enter any number");
    scanf("%lld");
    if(n<0)
    {
        printf("error! factorial of negative number does not exit");
        
    }for(i=0;i>=n;i++)
    
    {   n*=i;
        printf("%lld",n);
    }
}
